{% form_theme edit_form 'form_errors.html.twig' %}

{{ form_start(edit_form, {'action': path('otros_edit', { 'id': id})}) }}

<div class="panel panel-default">
    <div class="panel-body">
        <div class="row">

            <div class="col-lg-12">
                <div class="form-group">
                    <div>
                        {{ form_label(edit_form.actividad) }}
                    </div>
                    <div>
                        {{ form_widget(edit_form.actividad,{'attr': {'class': 'form-control input-sm', 'placeholder':'Actividad', 'rows':'5'} } ) }}
                    </div>
                    <div>{{ form_errors(edit_form.actividad) }}</div>
                </div>
            </div>

        </div>
    </div>
</div>

<div class="row">
    <div class="col-lg-7 col-lg-offset-5">
        <input type="submit" value="Guardar"  class="btn btn-primary"/>
        <button type="button" class="btn btn-warning" data-dismiss="modal">Cancelar</button>
        {{ form_end(edit_form) }}
    </div>
</div>