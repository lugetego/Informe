{% extends 'base.html.twig' %}

{% block wrapper %}
    <div class="row">

        <div class="col-lg-12">
            <p></p>
            {#                <h1 class="page-header">Solicitudes </h1>#}
        </div>
    </div>
    <div class="page-header">
        <h1>Proyectos</h1>
    </div>

    <div class="panel panel-default">
        <div class="panel-heading">
            <h3 class="panel-title">{{ proyecto.nombre}}</h3>
        </div>
        <div class="panel-body">

            <div class="row">
                <div class="col-md-4">
                    <dl>
                        <dt>Tipo de proyecto</dt>
                        <dd>{{ proyecto.tipo }}</dd>
                    </dl>
                </div>
                <div class="col-md-4">
                    <dl>
                        <dt>Número de proyecto</dt>
                        <dd>{{ proyecto.numero }}</dd>
                    </dl>
                </div>

            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-lg-7 col-lg-offset-5">

            {{ form_start(delete_form) }}
            <a class="btn btn-primary" href="{{ path('proyectos_edit', { 'id': proyecto.id }) }}">Modificar</a>
            <a class="btn btn-warning" href="{{ path('dashboard') }}">Cancelar</a>
            <input class="btn btn-danger" type="submit" value="Delete">
            {{ form_end(delete_form) }}

        </div>
    </div>




{% endblock %}
